vich_uploader:
    db_driver: orm
    
    metadata:
        type: annotation

    mappings:
        profile:
            uri_prefix: /images/profile
            upload_destination: '%kernel.project_dir%/public/images/profile'
